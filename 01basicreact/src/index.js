import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root')); // react creare his own DOM 
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
